# Public URL where agents reach the control plane.
# Leave unset to auto-start a Cloudflare quick tunnel (ephemeral).
# PRESSLUFT_CONTROL_PLANE_URL=https://yourname.pressluft.dev

# Ports (defaults shown).
# DEV_API_PORT=8081
# DEV_UI_PORT=8080
