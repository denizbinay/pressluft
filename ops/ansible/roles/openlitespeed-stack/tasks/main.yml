---
- name: Block unavailable openlitespeed-stack role
  ansible.builtin.fail:
    msg: >-
      The openlitespeed-stack profile is intentionally unavailable in the current
      platform contract. nginx-stack is the only fully convergent baseline in
      this milestone.
